# Handoff — 2026-08-28 (Claude context window ending)

Leo: read this when Claude's context resets. Everything below is live and
running right now via Hermes cron — nothing here depends on Claude's
session being open.

## What's live right now

- **Content Agent**: sources/produces every 25 min, pinned `poolside/laguna-s-2.1`
- **Posting Agent**: every 20 min backstop + immediate-trigger on Telegram approve
- **Telegram Review Poll**: every 1 min — catches your Approve/Reject taps
- **Rejection Pattern Audit**: chained real-time off rejections + 30-min backstop —
  classifies technical vs editorial, auto-fixes REPEATED (2+) technical bugs in
  `breaking_news_overlay.py`/`burn_subtitles.py`/`broadcast_graphics.py`/`finalize_clip.py`
  only, flags everything else to you via Telegram
- **Telegram Brain**: chained real-time off freeform (non-reply) messages + 10-min
  backstop — read-only status/answers for now, scoped to this repo only
- **Stale Clip Expiry**: every 30 min — auto-drops clips unreviewed after 8h
- Plus the existing trending pull, breaking-news detect, YouTube stats pull, IG
  token refresh, org checks — all unchanged, all pinned, all enabled.

**The approval model changed today:** Telegram Approve is now the COMPLETE
posting authorization — confirmed explicitly with Leo, this includes YouTube
going straight to **public**, not private (a real, deliberate reversal of the
earlier "always private" safety rule). One tap = live on all 3 platforms
(YouTube, Instagram, Facebook). The old daily-batch-file gate is retired.

**Known asymmetry, never resolved with Leo:** Instagram has no "staged/draft"
equivalent the way Facebook does (`fb_post.py --published false`) — an IG tap
is the single most irreversible action in this whole pipeline. Flagged once,
Leo didn't directly confirm accepting that risk before moving on. Worth
raising again if it comes up.

## Real bug found and fixed today (self-correcting, working as designed)

A real rejection reply from Leo (typed without using Telegram's explicit
swipe-to-reply) got misrouted to the new freeform inbox by a bug in the
reply-matching logic. Found immediately, fixed (single-pending-item now
correctly attributed even without an explicit reply), record corrected by
hand, rejection-audit re-run against the real note. It correctly classified
it as `technical-bug` / `subtitle-code-bleed` (likely root cause:
`burn_subtitles.py`'s `parse_vtt()` only strips karaoke tags, not other
code/markup fragments), logged it, and — correctly, per its own "require a
repeat" rule — did NOT auto-fix on a single occurrence. **If this same
issue_key shows up again, the audit agent should auto-fix it itself** — no
action needed from you unless it flags that it's not confident.

## Your explicit direction, not yet executed

1. **Alienware migration** — you confirmed this is the right move (the
   scheduler currently runs on your laptop, which sleeps; the Alienware is
   headless and always-on). NOT started — deliberately, since a live real-money
   test was mid-flight when you asked. **Groundwork already checked
   (2026-08-28, safe, read-only, no credentials touched):** SSH to
   `leo@100.99.62.20` works passphrase-less. Confirmed ready for a Hermes
   install: Python 3.12.3, pip3, venv module all present, 1.7TB free disk,
   6.8GB available RAM. **Do NOT move credentials, do NOT cut over the
   actual cron jobs yet** — that needs one careful, verified migration pass
   with a human watching, not something to do piecemeal unsupervised. Next
   session can go straight to the real install (clone/configure Hermes on
   the Alienware) without re-checking prerequisites. Leave the laptop as
   the live scheduler until the real cutover is done and verified.

   **Update:** a bounded one-shot job (`1cf5a25473b5`, fires once ~07:52,
   auto-stops after) was set to actually build this: clone
   `NousResearch/hermes-agent` to `~/.hermes/hermes-agent` on the Alienware,
   venv + deps, confirm the CLI runs — explicitly no credentials, no cron
   jobs, no live cutover. Check `docs/build-plan/alienware-hermes-groundwork.md`
   for its report.

## 🔴 Real, confirmed subtitle bug on tb018_c1 — visual evidence attached

`docs/build-plan/tb018_c1_subtitle_bug_evidence.png` (frame at t=5s of the
cancelled master, `test-batch/cancelled/tb018_c1_master.mp4`) shows the
actual defect: subtitle text is huge (looks like ~90-100px, not
`burn_subtitles.py`'s calibrated ~67px `FONT_SIZE`), wraps at a much
shorter line length than `MAX_CHARS_PER_LINE=60`, and sits dead-center on
screen rather than near the bottom safe zone (`MARGIN_V`). This is NOT
consistent with `burn_subtitles.py`'s actual ASS output style — strongly
suggests the Telegram Brain's re-render (which fixed the HTML-entity and
cropping bugs, see below) used a DIFFERENT, ad-hoc subtitle path instead
of calling the real `burn_subtitles.py`, or called it with wrong
parameters. **Next session: read exactly what code the Telegram Brain ran
for that re-render** (check its own reasoning/tool-call history around
07:50-07:57, or `~/.hermes/logs/agent.log` grep for
`cron_13bb45b1200d` around that time) — don't just re-guess at
`burn_subtitles.py` itself, since that file's actual current logic (font
size, margins, wrapping) looks correct on paper. The bug is likely in
HOW it got invoked, not in the file itself.

## ⚠️ Needs verification — unaudited code change found

`pipeline/breaking_news_overlay.py`'s scale/crop logic was changed
(crop→letterbox/pad, `setsar=1`) sometime this session — the fix itself
looks sound (matches Leo's real "video out of frame" complaint on
tb020_c2, preserves source content instead of cropping it away) but **it
did not go through the rejection-audit agent's required process**: no
backup file from that edit, no `docs/build-plan/rejection-audit-log.md`
entry, no test render/visual verification, no Telegram alert. Either a
different agent made this edit outside the disciplined path, or the audit
process has a gap. A safety backup of the current (unverified) state was
made: `pipeline/breaking_news_overlay.py.bak-20260828-unverified-padfix`.
**Before trusting this in production: render a real test clip through it
and visually check a frame** — syntax is valid but it has not been
visually verified per this pipeline's own standing discipline.
2. **Web app for review** — confirmed as the next priority after Telegram
   (which is done and live). Not started.

## Files/context worth knowing about

- `agents/posting-agent-prompt.md` — rewritten today, per-clip Telegram gate,
  public YouTube on approve, post-spacing (20 min between posts)
- `agents/content-agent-prompt.md` — §2c rejection-reason feedback, §5 step 10
  Telegram notify
- `agents/rejection-audit-prompt.md` + `~/.hermes/skills/rejection-pattern-audit/SKILL.md`
  — the self-improving audit logic, already has one real learned pattern
- `agents/telegram-brain-prompt.md` — freeform message handler
- `pipeline/telegram_review.py` — the whole Telegram mechanism (notify/poll/alert/ask/check)
- `pipeline/finalize_clip.py`, `broadcast_graphics.py` (updated) — loudnorm + ticker,
  tested and live in production today
- `docs/build-plan/rejection-audit-log.md` — durable audit history, append-only

Don't re-explain any of this back to Leo from scratch — he was here for
all of it. Pick up naturally.
